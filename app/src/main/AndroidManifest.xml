<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 12+（API 31+）BLE 运行时权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <!-- Android 11 及以下（API <=30）必需：普通权限，声明即授予；12+ 不再使用 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- API 26-28 写公共 Downloads 需要（API 29+ 走 MediaStore 免权限） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- ===== Wi-Fi Direct 增强（实验性）必需权限：缺失则 WifiP2pManager 调用抛 SecurityException 崩溃 ===== -->
    <!-- API 33+：NEARBY_WIFI_DEVICES 近似权限（替代位置权限），neverForLocation 表明不用于定位 -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <!-- API <=32 位置权限：API <=30 BLE 扫描必需；API 31-32 Wi-Fi Direct discoverPeers 也必需（33+ 由 NEARBY_WIFI_DEVICES 覆盖） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <!-- Wi-Fi Direct 数据通道走标准 TCP/UDP Socket，必须 INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Android 13+：仅在用户从界面主动启用消息提醒时请求，启动时不弹窗。 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 前台服务必需（Android 9+ targetSdk>=28 startForegroundService 校验；缺失启动即 SecurityException 崩溃） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 强制 FGS type 权限：connectedDevice 对应前台服务的 foregroundServiceType，缺失 startForeground 抛 SecurityException -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:name=".MeshChatApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_meshchat"
        android:label="MeshChat"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_meshchat"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeshChat"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".MeshChatService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
